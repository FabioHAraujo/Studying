const [a, b, c, d] = [3, 5, 1, 15];

const soma = a + b + c + d;
const subratracao = d - b;
const multiplicacao = a * b;
const divisao = d / a;
const modulo = a % 2;

// Operador unário que transforma em negativo:
// -a

console.log(soma, subratracao, multiplicacao, -divisao, modulo);
