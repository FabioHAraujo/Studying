USE ptctestes;
CREATE TABLE clientesptc (
    ID          VARCHAR(11),
    CLIENTE     VARCHAR(100),
    BLOQUEIO    VARCHAR(30),
    CIDADE      VARCHAR(100),
    MAC         VARCHAR(100),
    PTA         VARCHAR(100),
    IP          VARCHAR(100),
    USER        VARCHAR(100)
);